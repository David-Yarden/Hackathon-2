import pkg from "pg";
const { Pool } = pkg;

const pool = new Pool({
  host: "localhost",
  user: "postgres",
  password: "YOUR_PASSWORD",
  database: "budget_tracker",
  port: 5432,
});

export default pool;
